package com.rapidphotoupload.features.photoquery.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Controller for photo query feature slice.
 * This is an example of a vertical slice controller.
 * Full implementation will be completed in Epic 4.
 */
@RestController
@RequestMapping("/api/v1/photos")
public class PhotoQueryController {
    // TODO: Implement endpoints in Epic 4 - Photo Query API
    // - GET /api/v1/photos
    // - GET /api/v1/photos/{photoId}
    // - GET /api/v1/photos/{photoId}/download
}

